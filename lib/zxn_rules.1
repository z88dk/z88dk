	call	l_mult
=
	mul

	call	l_inclong
=
	inc	dehl

	call	l_declong
=
	dec	dehl

	ld	bc,%1
	add	hl,bc
=
	add	hl,+(%1)

	ld	hl,0	;const
	add	hl,sp
=
	ld	hl,sp

	ld	hl,%1	;const
	push	hl
	ld	hl,%2
=
	push	%1	;const
	ld	hl,%2

	push	%1	;const
=
	defb	0xeb,0x8a, +(%1 % 256), +(%1 / 256)	; push %1

	add	hl,+(%1)
=
	defb	0xed, 0x34, +(%1 % 256), +(%1 / 256) ; add hl,+(%1)

	ld	hl,sp
=
	defb	0xed, 0x25	; ld hl,sp

	mul
=
	defb	0xed, 0x30	; mul

	inc	dehl
=
	defb	0xed, 0x37	; inc dehl

	dec	dehl
=
	defb	0xed, 0x38	; dec dehl
