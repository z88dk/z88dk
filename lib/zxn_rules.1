	call	l_mult
=
	mul

	call	l_inclong
=
	inc	dehl

	call	l_declong
=
	dec	dehl

	ld	bc,%1
	add	hl,bc
=
	add	hl,+(%1)

	ld	hl,0	;const
	add	hl,sp
=
	ld	hl,sp

	ld	hl,%1	;const
	push	hl
	ld	hl,%2
=
	push	%1	;const
	ld	hl,%2

	push	de
	push	hl
	ld	hl,%1	;const
	ld	de,0
	call	l_long_add
=
	add	dehl,%1	;const

	push	de
	push	hl
	ld	hl,%1	;const
	ld	de,65535
	call	l_long_add
=
	sub	dehl,+(65536 - %1)	;const

	ld	hl,%1	;const
	ld	de,%2
	push	de
	push	hl
	ld	hl,%3
=
	push	%1
	push	%2
	ld	hl,%3
