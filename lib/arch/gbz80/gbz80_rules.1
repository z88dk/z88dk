	call	l_gint%1
=
	ld	a,(hl+)
	ld	h,(hl)
	ld	l,a

%check 0 <= %1 <= 127
	ld	hl,%1	;const
	add	hl,sp
=
	ld	hl,sp+%1

	ld	hl,%1
	ex	de,hl
=
	ld	e,l
	ld	d,h
	ld	hl,%1

	ld	hl,(%1)
=
	ld	hl,%1
	ld	a,(hl+)
	ld	h,(hl)
	ld	l,a

	ld	a,(hl+)
	ld	h,(hl)
	ld	l,a
	ld	d,h
	ld	e,l
	pop	hl
=
	ld	a,(hl+)
	ld	d,(hl)
	ld	e,a
	pop	hl
