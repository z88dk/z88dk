#
# Target configuration file for z88dk
#

# Asm file which contains the startup code (without suffix)
CRT0		 DESTDIR/lib/target/vz700/classic/vz700_crt0

# Any default options you want - these are options to zcc which are fed
# through to compiler, assembler etc as necessary
OPTIONS		 -O2 -SO2 -iquote. -DZ80 -D__VZ700__ -M -subtype=default -clib=default

CLIB		default -lvz700_clib -lndos -Cc-standard-escape-chars 

SUBTYPE		default -Cz+vz700 -startup=1
SUBTYPE		rom -Cz+rom -Cz-s -Cz32768 -Cz--rombase=0 -startup-2

