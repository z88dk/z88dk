#
# Target configuration file for z88dk
#

# ZX has a custom maths library
Z88MATHFLG      -D__NATIVE_MATH__
Z88MATHLIB      mzx

# Asm file which contains the startup code (without suffix)
CRT0		 DESTDIR\lib\spec_crt0

# Any default options you want - these are options to zcc which are fed
# through to compiler, assembler etc as necessary
OPTIONS		 -v -O2 -I. -DZ80 -DSPECTRUM -D__SPECTRUM__ -M -subtype=default

SUBTYPE		default -lzx_clib -Cz+zx
SUBTYPE		rom	-lzx_clib -Cz+rom -Cz-s -Cz16384 -startup=2
SUBTYPE		ansi -lzxan_clib -Cz+zx
