#
# Target configuration file for z88dk
#

# Asm file which contains the startup code (without suffix)
CRT0		 DESTDIR/lib/test_crt0

# Any default options you want - these are options to zcc which are fed
# through to compiler, assembler etc as necessary
OPTIONS		 -v -O2 -iquote.  -DZ80 -M -subtype=default -clib=default

CLIB      default -Cc-standard-escape-chars -ltest_clib 
CLIB	  rabbit  -mr2k -Cc-standard-escape-chars -ltestrcm_clib  -DRCMX000 -D__RCMX000__ -startuplib=rcmx000_crt0 -custom-copt-rules=DESTDIR/lib/rabbit_rules.1


SUBTYPE   default -Cz+test
