#
# Target configuration file for z88dk
#

INCLUDE tools.inc

WARNING This target is not complete and probably won't work

# Asm file which contains the startup code (without suffix)
CRT0		 DESTDIR/lib/target/phc20/classic/phc20_crt0

# Any default options you want - these are options to zcc which are fed
# through to compiler, assembler etc as necessary
OPTIONS		 -O2 -SO2 -iquote. -D__Z80__ -D__Z80 -DPHC20 -D__PHC20__ -D__HAVE_GENCON -pragma-define:__HAVE_GENCON=1 -Cc-standard-escape-chars -subtype=default -clib=default 

CLIB		default -lphc20_clib -lndos -LDESTDIR/lib/clibs/z80

SUBTYPE   default  -Cz+phc20

INCLUDE alias.inc

