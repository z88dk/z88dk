#
# Target configuration file for z88dk
#

INCLUDE tools.inc

WARNING This target is not complete and probably won't work

# Asm file which contains the startup code (without suffix)
CRT0		 DESTDIR/lib/target/astrocde/classic/astrocde_crt0

# Any default options you want - these are options to zcc which are fed
# through to compiler, assembler etc as necessary
OPTIONS		 -O2 -SO2 -iquote. -D__Z80__ -D__Z80 -D__ASTROCDE -subtype=default -clib=default

#CLIB		default -lastrocde_clib -lndos -Cc-standard-escape-chars -LDESTDIR/lib/clibs/z80
CLIB		default -lndos -Cc-standard-escape-chars -LDESTDIR/lib/clibs/z80

SUBTYPE     none 
SUBTYPE     default -Cz+rom -Cz--romsize=8192 -Cz--rombase=8192


INCLUDE alias.inc
