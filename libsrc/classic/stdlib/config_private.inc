; Satisfies the INCLUDE "config_private.inc" in the shared newlib sort cores
; (asm_quicksort.asm) when they are pulled into the classic library via
; qsort_core.asm.  The sort options are set with defc in qsort_core.asm, so
; this only needs to exist.
