## Compile

```
Use the makefile
```

## Run

These keys are active once the sna is launched:

```
h   fewer hardware sprites
H   more hardware sprites
```

## Description

Up to 64 hardware sprites are pushed around with the cpu running at 14MHz.  The layer priority is set so 
that layer 2 is on bottom. Layer 2 is scrolled in the interrupt routine.

